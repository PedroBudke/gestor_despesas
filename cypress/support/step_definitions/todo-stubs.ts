import { Given } from "@badeball/cypress-cucumber-preprocessor";

// Step provisorio usado nos cenarios @todo (Given/When/Then/And todos usam este mesmo registro).
// Falha intencionalmente para evidenciar que a feature ainda nao foi implementada.
Given("pendente de implementacao", () => {
  throw new Error("TODO: este cenario ainda nao foi implementado.");
});